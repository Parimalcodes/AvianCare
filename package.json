{
  "name": "aviancare:-bird-diet-&-med-tracker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:web": "vite build",
    "android:sync-assets": "node scripts/sync-android-assets.mjs",
    "build:android": "npm run build:web && npm run android:sync-assets",
    "android:studio": "echo \"Open the ./android folder in Android Studio and run the app\"",
    "android:zip": "npm run build:android && node scripts/package-android-zip.mjs"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "@google/genai": "^1.35.0",
    "lucide-react": "^0.562.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
